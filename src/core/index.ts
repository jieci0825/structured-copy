export { formatRange } from './format-range';
export { formatSelection } from './format-selection';
export { normalizeLanguage } from './normalize-language';
export { resolveFilePath } from './resolve-file-path';

export type { StructuredSelectionContext } from './format-selection';
